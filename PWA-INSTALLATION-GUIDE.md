## 📱 Install FairRoute AI as PWA

Your app is now configured as a **Progressive Web App**! Here's how to install it:

### **On Android (Chrome/Edge):**
1. Open the app in Chrome or Edge browser
2. Tap the **⋮** menu (three dots)
3. Select **"Add to Home Screen"** or **"Install app"**
4. Tap **"Install"** in the prompt
5. The app icon will appear on your home screen!

### **On iOS (Safari):**
1. Open the app in Safari
2. Tap the **Share** button (square with arrow)
3. Scroll and tap **"Add to Home Screen"**
4. Name it and tap **"Add"**
5. The app will appear on your home screen!

### **On Desktop (Chrome/Edge):**
1. Open the app in Chrome or Edge
2. Click the **install icon** (⊕) in the address bar
3. Click **"Install"**

---

## 🎨 **Next Steps - Add App Icons:**

You need to create two icon files:
- **icon-192.png** (192x192 pixels)
- **icon-512.png** (512x512 pixels)

Place them in the `public` folder. You can:
1. Use an online tool like **Canva** or **Figma** to create them
2. Use your logo with the violet/purple theme (#8b5cf6)
3. Or use a simple icon generator

---

## ✅ **Features Enabled:**
- ✅ Installable on mobile & desktop
- ✅ Works offline (basic caching)
- ✅ Full-screen experience
- ✅ Appears in app drawer
- ✅ Fast loading
- ✅ Mobile-optimized UI

---

## 🚀 **To Test:**
1. Build the production app: `npm run build`
2. Start production server: `npm start`
3. Open on mobile browser
4. Look for "Install" or "Add to Home Screen" prompt

**Note:** PWA features only work in production mode, not in development (`npm run dev`).
